import { DropedMenu } from "core/components";
import React          from "react";
import "./Navbar.scss";

const Navbar = () => {
	return (
		<div id="Navbar">
			<DropedMenu />
		</div>
	);
};

export default Navbar;
